#include<stdio.h>
#define MAX 10
int queue[MAX];
int front =-1;rear=-1;
void enqueue(int value)
{
    if (rear==MAX-1)
    {
        printf("The Queue is Overflow");
    }
    else
    {
        rear++;
        front=0;
        queue[rear]=value;
        printf("The %d is added into the Queue",value);
    }
}
void dequeue()
{
    if(front==-1||front>rear)
    {
    printf("Queue is Underflow");
    }
    else
    {
        printf("%d is Deleted from the queue",queue[front]);
        front++;
    }
}
void display()
{
    if("")
